import {Link} from 'react-router-dom';import {useAuth} from '../../context/AuthContext';import {toggleFavorite,useFavoriteIds} from '../../hooks/useFavorites';import {rupiah} from '../../utils/format';
export default function ProductCard({product}){const {user}=useAuth();const ids=useFavoriteIds(user?.uid);const saved=ids.has(product.id);const fav=async(e)=>{e.preventDefault();if(!user)return alert('Silakan Sign In terlebih dahulu.');try{await toggleFavorite(user.uid,product,saved)}catch(err){alert(err.message)}};return <article className="product-card"><Link to={`/product/${product.id}`}><img src={product.imageUrl} alt={product.title}/></Link><div className="product-body"><div className="card-row"><span className="badge">{product.status==='in_transaction'?'Ordered':product.status==='sold'?'Sold':`Stock ${product.stock}`}</span><button className={`heart ${saved?'saved':''}`} onClick={fav} aria-label="Favorite">♥</button></div><h3>{product.title}</h3><strong>{rupiah(product.price)}</strong><p className="muted">{product.sellerName||'Seller'}</p><Link className="button" to={`/product/${product.id}`}>Lihat</Link></div></article>}
