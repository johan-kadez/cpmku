import {Outlet,useNavigate} from 'react-router-dom';import AdminNav from '../../components/admin/AdminNav';import {useAuth} from '../../context/AuthContext';export default function AdminShell(){const {logout}=useAuth();const navigate=useNavigate();const doLogout=async()=>{await logout();navigate('/admin/login',{replace:true})};return <div className="admin-shell"><header className="admin-header"><span className="brand">JOHAN<span>ADMIN</span></span><button onClick={doLogout}>Logout</button></header><div className="container"><section className="page-title"><h1>Admin Panel</h1><p>Kontrol marketplace tanpa custom claim. Admin ditentukan dari email environment backend.</p></section><AdminNav/><Outlet/></div></div>}
