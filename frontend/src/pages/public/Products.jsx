import {useMemo,useState} from 'react';import {useProducts} from '../../hooks/useProducts';import ProductGrid from '../../components/product/ProductGrid';export default function Products(){const {products,error}=useProducts();const[q,setQ]=useState('');const filtered=useMemo(()=>products.filter(p=>`${p.title} ${p.description}`.toLowerCase().includes(q.toLowerCase())),[products,q]);return <section><div className="section-head"><div><span className="eyebrow">MARKETPLACE</span><h1>Semua Produk</h1></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari produk..."/></div>{error&&<div className="notice error">{error}</div>}<ProductGrid products={filtered}/></section>}
