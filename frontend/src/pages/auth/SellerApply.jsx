import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAuth} from '../../context/AuthContext';import {api} from '../../services/api';
export default function SellerApply(){const{user,login}=useAuth();const nav=useNavigate();const[f,setF]=useState({name:user?.displayName||'',phone:'',email:user?.email||'',reason:'',photoUrl:user?.photoURL||''});const[busy,setBusy]=useState(false);if(!user)return <section className="auth-card"><h1>Daftar Seller</h1><p>Sign In Google terlebih dahulu sebelum mengirim pengajuan.</p><button className="button primary" onClick={()=>login().catch(e=>alert(e.message))}>Login Google</button></section>;const submit=async e=>{e.preventDefault();setBusy(true);try{await api('/sellers/apply',{method:'POST',body:JSON.stringify(f)});alert('Pengajuan seller berhasil dikirim dan menunggu Admin.');nav('/profile')}catch(err){alert(err.message)}finally{setBusy(false)}};return <form className="form-card" onSubmit={submit}><span className="eyebrow">LOGIN AS SELLER · SIGN UP</span><h1>Daftar Seller</h1><label>Nama Seller<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Nomor WhatsApp Seller<input required value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><label>Email Seller<input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Tujuan menjadi Seller<textarea required value={f.reason} onChange={e=>setF({...f,reason:e.target.value})}/></label><label>URL Foto Profile<input required type="url" value={f.photoUrl} onChange={e=>setF({...f,photoUrl:e.target.value})}/></label><button className="button primary" disabled={busy}>{busy?'Mengirim...':'Daftar'}</button></form>}
