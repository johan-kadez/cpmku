import {db,FieldValue} from '../firebase/admin.js';import {HttpError} from '../utils/errors.js';
export async function applySeller(uid,authEmail,data){const seller=await db.collection('sellers').doc(uid).get();if(seller.exists&&seller.data().status==='approved')throw new HttpError(409,'Akun ini sudah menjadi seller.');const ref=db.collection('registrations').doc(uid);const old=await ref.get();if(old.exists&&old.data().status==='pending')throw new HttpError(409,'Pengajuan seller masih pending.');const name=String(data?.name||'').trim(),phone=String(data?.phone||'').trim(),email=String(data?.email||authEmail||'').trim().toLowerCase(),reason=String(data?.reason||'').trim(),photoUrl=String(data?.photoUrl||'').trim();if(!name||!phone||!email||!reason||!photoUrl)throw new HttpError(400,'Nama, WhatsApp, email, tujuan, dan URL foto wajib diisi.');try{new URL(photoUrl)}catch{throw new HttpError(400,'URL foto profile tidak valid.')}if(authEmail&&email!==authEmail.toLowerCase())throw new HttpError(403,'Email seller harus sesuai dengan akun Google yang login.');await ref.set({uid,email,name,phone,reason,photoUrl,status:'pending',createdAt:old.exists?old.data().createdAt:FieldValue.serverTimestamp(),updatedAt:FieldValue.serverTimestamp()},{merge:true});return{ok:true,status:'pending'};}
