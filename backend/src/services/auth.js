import {env} from '../config/env.js';import {db,FieldValue} from '../firebase/admin.js';export async function currentUser(user){const email=(user.email||'').toLowerCase();const profileRef=db.collection('users').doc(user.uid);const[seller,profile]=await Promise.all([db.collection('sellers').doc(user.uid).get(),profileRef.get()]);if(!profile.exists)await profileRef.set({uid:user.uid,email,name:user.name||email.split('@')[0]||'User',photoUrl:user.picture||'',banned:false,createdAt:FieldValue.serverTimestamp(),updatedAt:FieldValue.serverTimestamp()},{merge:true});let role='buyer';if(env.adminEmails.includes(email))role='admin';else if(seller.exists&&seller.data().status==='approved'&&!seller.data().banned)role='seller';return{uid:user.uid,email,name:user.name||email.split('@')[0]||'User',role,banned:Boolean(profile.data()?.banned)}}
